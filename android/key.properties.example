# Copy this file to android/key.properties after creating the permanent keystore.
# Never commit android/key.properties or the keystore itself.
storeFile=panpanskii-release.jks
storePassword=REPLACE_WITH_YOUR_KEYSTORE_PASSWORD
keyAlias=panpanskii
keyPassword=REPLACE_WITH_YOUR_KEY_PASSWORD
